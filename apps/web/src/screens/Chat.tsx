/**
 * One conversation, and the composer before a chat exists (docs/specs/m4-3-chat-web.md §3.1). Built
 * in Steps 3 and 4; this is the route's shape only, so Step 1's navigation resolves.
 */
export function Chat() {
  return null;
}
