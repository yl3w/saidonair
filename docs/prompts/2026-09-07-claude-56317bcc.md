# Which skills are installed

2026-09-07 14:53 · Claude Code · 1 prompt, 3 turns · session `56317bcc`

<!-- capture:covers claude-56317bcc -->

A single question, kept for completeness.

---

### Asked · 2026-09-07 14:53

```text
what skills are installed
```

### Replied · 2026-09-07 14:53

```text
I'll check the repo's `skills/` directory and how they get installed, then list what's available in this session.
```

### Replied · 2026-09-07 14:54

```text
This repo ships one skill, and it is installed. Here is the full picture of what this session can use.

**Project skill (from `skills/`, installed via `pnpm skills:install`)**
- `clean-local-do`: wipes the local `wrangler dev` Durable Object storage under `apps/api/.wrangler/state/v3/do`. It only runs when you explicitly ask, refuses while the dev server is up, and never touches deployed resources or `.dev.vars`.

The installer placed it at `.agents/skills/clean-local-do` with a symlink from `.claude/skills/`. No Cursor or Codex skill directories exist yet, so only Claude Code sees it right now.

**User-level skill (from `~/.claude/skills`)**
- `find-skills`: discovers and installs agent skills on request.

**Plugins installed at user scope**
- `sonarqube` 2.5.0, which contributes the `sonar-*` skills: analyze, coverage, dependency-risks, duplication, fix-issue, integrate, list-issues, list-projects, quality-gate, plus a `sonarqube-reviewer` agent.
- `typescript-lsp` 1.0.0, which adds language server support rather than skills.

**Built-in Claude Code skills** available in this session include `code-review`, `simplify`, `security-review`, `run`, `init`, `design`, `dataviz`, `loop`, `schedule`, `claude-api`, `update-config`, `keybindings-help`, `fewer-permission-prompts`, `workflow-authoring`, and the three `artifact-*` helpers.
```
