# A multi-role review of the PRD

2026-09-12 16:42 · Codex · 2 prompts, 19 turns · session `01a0975a`

<!-- capture:covers codex-01a0975a -->

The same three-role reading applied to PRD.md itself.

---

### Asked · 2026-09-12 16:42

```text
please do a review of prd.md in the current working directory. Review in multiple roles (a) as a
  PM review product functionality (b) as a designer review UX (c) as an engineer review system design /
  coupling and architecture.
```

### Replied · 2026-09-12 16:42

```text
I’m using the repo’s `using-superpowers` workflow first, then I’ll split the review across the three requested lenses and reconcile them against the PRD’s own priorities and current repository structure. This is analysis-only: I won’t edit files.
```

### Replied · 2026-09-12 16:42

```text
The three lenses are independent enough to run in parallel, so I’m using the repo’s parallel-review skill: one reviewer each for product, UX, and architecture. I’ll independently check document consistency and current implementation context, then combine only actionable, evidence-backed findings with exact anchors.
```

### Replied · 2026-09-12 16:42

```text
I’m using the `using-superpowers` workflow to honor the repository’s skill rules, then I’ll do an analysis-only, product-focused read of the full AGENTS and PRD. I’ll check only directly relevant specs/code where a PRD claim needs validation; I won’t modify files.
```

### Replied · 2026-09-12 16:42

```text
I’m using the repo’s `using-superpowers` skill to follow its prescribed review workflow. I’ll keep this analysis-only and ground every finding in the current PRD, supporting specs, and only the UI code needed to verify live behavior.
```

### Replied · 2026-09-12 16:42

```text
I’m using the repository’s `using-superpowers` guidance to structure this analysis-only review, then I’ll read the working rules and PRD in full before tracing the highest-risk implementation paths. I won’t edit files or make network calls.
```

### Replied · 2026-09-12 16:42

```text
I’m now reading the PRD end to end and checking the parts most likely to hide lifecycle contradictions: channel request/approval, ingestion recovery, follow state, digest freshness, chat eligibility, retention/deletion, and operational acceptance gates. I’ll treat earlier decisions as historical context only and verify them against today’s files.
```

### Replied · 2026-09-12 16:42

```text
I’ve found earlier review context, but I’m treating it as hypotheses rather than current truth. The current PRD is 806 lines and both supporting UX specs are substantial, so I’m reading them end to end before checking implementation details or forming findings.
```

### Replied · 2026-09-12 16:43

```text
I’ve completed the repository rules and memory cross-check. The review will treat the PRD as the target contract and distinguish true PRD architecture defects from functionality that is simply not implemented yet; prior rollout notes are being used only as historical pointers, not as current evidence.
```

### Replied · 2026-09-12 16:43

```text
The first clear release risk is structural: the PRD’s “private per-user data” promise conflicts with identity being a caller-supplied email and with CORS explicitly described as non-protective. I’m now checking the subordinate specs and live route/UI surfaces for additional lifecycle or launch-readiness gaps rather than treating target requirements as implemented behavior.
```

### Replied · 2026-09-12 16:45

```text
The main UX risks are now converging around three areas: ambiguous “new” digest timing, attention links/actions that do not match the owner’s intended destination, and missing interaction contracts for chat/accessibility. I’m doing a final cross-check against the current components so I can separate PRD defects from known M3/M4 implementation lag.
```

### Replied · 2026-09-12 16:45

```text
The first concrete divergence is substantial but expected by the milestone note: the checked-in runtime and schemas are still the pre-redesign ingestion model, while the PRD defines the M3 target. I’m now separating “planned but absent” from risks the target design itself does not close, especially cross-DO follow consistency and concurrent episode starts/publication.
```

### Replied · 2026-09-12 16:45

```text
A few concrete tensions are emerging: the PRD promises privacy and owner-only actions while identity is a caller-supplied email; follow state is duplicated across two Durable Objects without an automatic repair path; and several subordinate specs still expose a removed `waiting` count. I’m checking whether these are isolated wording defects or real contract gaps before assigning severity.
```

### Replied · 2026-09-12 16:46

```text
No P0 findings.

## P1

1. **[Release gap] The current UI contract still exposes the superseded ingestion model.**  
   Anchors: `docs/PRD.md:252-254`, `docs/PRD.md:277-291`, `docs/PRD.md:630-635`; `packages/shared/src/index.ts:162-175`, `packages/shared/src/index.ts:178-192`, `apps/web/src/components/CatalogHealth.tsx:36-40`, `apps/web/src/screens/OwnerChannel.tsx:105-139`, `apps/web/src/screens/OwnerChannel.tsx:300-315`.  
   Impact: owners currently see a channel-level waiting count and old run outcomes, while Retry is limited to failed/skipped episodes on approved channels. The target mental model instead puts reasons on attempts, makes discovery runs feed-only, and permits Retry for every episode/channel state. A browser walkthrough of the current UI therefore cannot validate the PRD.  
   Recommendation: make the M3 UI migration an explicit release gate: remove `waiting`/active-run concepts, show latest-attempt and recovery-window state, render feed-only run results, and test Retry/Skip across requested, approved, paused, and declined channels.

2. **[Gap] Chats and preferences lack a shippable interaction contract.**  
   Anchors: `docs/PRD.md:396-412`, `docs/PRD.md:450-458`, `docs/PRD.md:601-603`, `docs/PRD.md:684-687`, `docs/PRD.md:793-804`.  
   Impact: the PRD promises multiple conversations and preference-aware answers, and even models pending/failed messages, but defines no way to edit preferences and no chat-list ordering, title behavior, empty state, pending state, failure/retry flow, composer keyboard behavior, or source presentation. M4/M5 could satisfy the API while remaining unusable.  
   Recommendation: add a chat screen contract and acceptance criteria covering create/select/title, ordering, empty and no-follow states, send/pending/failure/retry, source links and historical citations, preference editing, and Enter versus Shift+Enter. Resolve the open pinned-composer decision before implementation.

3. **[Gap] Accessibility is not a release criterion.**  
   Anchors: `docs/PRD.md:563-569`, `docs/PRD.md:694-750`; `docs/specs/home-read-experience.md:653-680`; `apps/web/src/components/RequestQueue.tsx:145-196`, `apps/web/src/components/RequestQueue.tsx:230-247`.  
   Impact: “text only” does not ensure accessibility. Nothing requires keyboard completion, focus placement after dynamic forms/list reloads, announcement of loading/errors/action results, zoom/reflow, or readable tables. In the current queue, an Approve form is inserted before the focused trigger, so normal forward tabbing skips its fields.  
   Recommendation: add M5 acceptance checks for keyboard-only completion, intentional focus after form expansion and row removal, `aria-live`/status announcements, semantic headings and table labeling, visible focus, 200% zoom, narrow-viewport reflow, contrast, and no page-level horizontal scrolling.

## P2

4. **[Defect] The owner attention CTA bypasses one of the issues it counts.**  
   Anchors: `docs/PRD.md:576-580`, `docs/PRD.md:611-618`; `apps/web/src/components/OwnerCard.tsx:9-20`.  
   Impact: requested channels increase both the badge and card count, but “Review” always lands at `#attention`; requests live under `#requests`. An owner following the alert can land where the reported work is absent.  
   Recommendation: make each count its own link to `#requests` or `#attention`, or route a single CTA to the first nonzero section.

5. **[Defect] “Today’s digest” presents summary-availability ordering using only publication-age copy.**  
   Anchors: `docs/PRD.md:180-186`, `docs/PRD.md:384-388`, `docs/PRD.md:581-585`; `docs/specs/home-read-experience.md:145-153`.  
   Impact: initial import can make older videos newly available today. A months-old video can therefore appear near the top under “Today’s digest” labeled only “3 months ago,” making the ordering look stale or broken. The seven-day view also retains the “Today’s digest” heading.  
   Recommendation: name the section “New summaries,” adapt the range heading, and show both “summarised X ago” and “published Y” when they materially differ.

6. **[Gap] Digest empty states collapse active recovery, no usable episodes, and a quiet period.**  
   Anchors: `docs/PRD.md:196-206`, `docs/PRD.md:256-270`, `docs/PRD.md:585-591`; `docs/specs/home-read-experience.md:159-169`, `docs/specs/home-read-experience.md:670-673`.  
   Impact: after approval, polling stops even though first summaries may take minutes or recover for 48 hours. “No new summaries” does not tell the reader whether work is underway, all tracked episodes were skipped, the feed was unavailable, or the period is simply quiet; Refresh is offered without explaining when it helps.  
   Recommendation: derive a reader-safe state from followed channel/episode counts: “Preparing summaries,” “No summarizable episodes yet,” or “Nothing new in this period,” with linked channel rows and a visibly labeled last-refresh time.

7. **[Defect] Owner Home silently turns the ordinary Add action into approval plus paid ingestion.**  
   Anchors: `docs/PRD.md:146-148`, `docs/PRD.md:592-600`; `docs/specs/home-read-experience.md:48-55`; `apps/web/src/components/AddChannel.tsx:44-65`.  
   Impact: the same “Add” control means “request and follow” for a user but “approve, follow, and immediately import up to five episodes” for the owner. Nothing near the Home control communicates the different consequence or import count.  
   Recommendation: make the owner variant explicit—“Add and start import,” display the import count and credit consequence, or restrict owner creation to the richer Owner form.

8. **[Defect] Unfollowing a declined channel is treated as reversible when it is not locally reversible.**  
   Anchors: `docs/PRD.md:345-361`, `docs/PRD.md:592-600`; `docs/specs/home-read-experience.md:663-669`.  
   Impact: the row disappears from Followed, declined channels are absent from Catalog, and ordinary Follow is forbidden. Recovering it requires remembering/pasting the ID and globally requesting it again, not simply undoing the unfollow.  
   Recommendation: confirm this specific case or provide an inline Undo/“Recently unfollowed” affordance that restores the follow without changing catalog status. Explain when an unfollow will system-pause the last-followed approved channel.

9. **[Gap] Read state is committed by a GET before successful presentation.**  
   Anchors: `docs/PRD.md:389-394`, `docs/PRD.md:681-683`, `docs/PRD.md:739-741`.  
   Impact: a response lost after the server commits, a render crash, or speculative fetching can clear unread state even though the person never sees the summary. This undermines the promise that content stays unread until actually returned/viewed.  
   Recommendation: return `wasUnread` without mutation, then acknowledge the rendered IDs idempotently after successful display; viewport-based acknowledgement would be stronger if “read” is intended literally.

10. **[Gap] Catalog filters have no defined clear/current-state behavior.**  
    Anchors: `docs/PRD.md:623-629`; `docs/specs/home-read-experience.md:292-299`, `docs/specs/home-read-experience.md:316-329`; `apps/web/src/components/CatalogHealth.tsx:19-35`.  
    Impact: each count narrows the table, but the PRD provides no “All” reset, selected-filter indication, back/reload behavior, or empty filtered state. Owners can become stranded in a subset or misread it as the whole catalog.  
    Recommendation: specify an All control, visible selected state, filtered-result copy, and preferably query-string persistence so Back and reload preserve intent.

11. **[Gap] “Start” and “Retry” overload materially different owner actions.**  
    Anchors: `docs/PRD.md:619-628`, `docs/PRD.md:277-285`, `docs/PRD.md:630-635`.  
    Impact: “Start” appears even after a channel has run and actually means “check the feed now.” “Retry” on an available episode starts a replacement and consumes provider/AI work, unlike retrying a failure. Owners can trigger expensive work under misleading labels.  
    Recommendation: use “Check feed now” for discovery; use “Retry processing” for pending/failed/skipped and “Reprocess” for available content, with concise consequence text for replacement.

## P3

12. **[Defect] First-time Home duplicates the complete Catalog.**  
    Anchors: `docs/PRD.md:576-580`, `docs/PRD.md:587-598`; `apps/web/src/screens/Home.tsx:114-168`, `apps/web/src/components/Digest.tsx:35-44`.  
    Impact: a user with no follows sees the same channels and Follow controls inside Digest and again immediately under Channels, increasing page length and creating two apparent sources of truth.  
    Recommendation: keep one Catalog instance; the digest empty state can link/scroll to it or the Channels section can be suppressed while it is embedded.

13. **[Defect] “Reviewed history” promises more history than the model retains.**  
    Anchors: `docs/PRD.md:154-162`, `docs/PRD.md:428-438`, `docs/PRD.md:614-616`.  
    Impact: only the latest review fields exist, so repeated decline/re-request/approve cycles overwrite earlier decisions. Calling the collapsed list “history” can mislead the owner during curation.  
    Recommendation: rename it “Latest decisions,” or add an actual review-event model if audit history is intended.

Optional enhancement: make owner filters URL-backed and add human-readable explanations beside technical attempt codes; neither should block the core UX fixes above.

## Strengths

- The approval state, episode content state, and recovery state are cleanly separated.
- Reader eligibility and owner visibility are consistently defined, including declined/paused behavior.
- Explicit Request-again and approved-channel withdrawal confirmation correctly match the scope of those changes.
- The retained-stale-row policy for Owner refreshes is unusually well specified.
- The basic IA—Account → Home/Channel and separate Owner/Owner-channel detail—is compact and understandable.
- Per-section loading/error isolation and deep-link requirements provide a good foundation.

## Unverified

Analysis only; no files were edited and no tests or browser walkthroughs were run. I did not validate keyboard navigation, assistive-technology announcements, responsive tables, contrast, deep-link behavior, or runtime performance. M3’s target owner/recovery UI and M4 chats are not implemented in the current components, so those flows could only be reviewed as specifications.

<oai-mem-citation>
<citation_entries>
MEMORY.md:202-220|note=[prior review scope and cross-document review preference]
rollout_summaries/2026-09-07T20-44-59-F494-home_owner_product_spec_review.md:26-40|note=[prior attention-routing hypothesis and historical context]
rollout_summaries/2026-09-09T04-06-19-v5AE-m3_ingestion_ux_contract_alignment.md:17-32|note=[approved digest availability and recovery decisions]
</citation_entries>
<rollout_ids>
01a07d9d-f75c-7951-b2bd-50039e3c4b2a
01a08458-61f3-74a2-bd5d-e209b5dfacdc
</rollout_ids>
</oai-mem-citation>
```

### Replied · 2026-09-12 16:47

```text
No P0 findings.

## Findings

### P1 — Confirmed: network clients can impersonate any user or the owner

The product claims a “small, trusted set,” private per-user data, and isolation, but any caller chooses `X-User-Email`; unknown addresses auto-register; CORS is explicitly not a guard; and that same identity selects the User DO and owner role. [docs/PRD.md:19](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:19), [docs/PRD.md:51](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:51), [docs/PRD.md:59](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:59), [docs/PRD.md:71](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:71), [apps/api/src/middleware/user.ts:15](/Users/bhaskar/workspace/media-digest-assistant/apps/api/src/middleware/user.ts:15).

Impact: on a network-accessible deployment, a caller who supplies another email can read that person’s chats/preferences/receipts; supplying an owner email enables catalog mutations. The success claim that users cannot inspect another user’s data is therefore only true for cooperative clients, not as an enforced property. [docs/PRD.md:696](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:696)

Recommendation: make this an explicit go/no-go decision before deployment. Either add genuine access control/private ingress, or explicitly define a trusted-network threat model and remove the privacy/isolation claims that the implementation cannot enforce. Also define owner rotation and revocation; promote-only seeding otherwise accumulates owner authority indefinitely.

### P1 — Confirmed: there is no defined usable-release cut line, and status labeling is ambiguous

The PRD says M3/M4 are unbuilt, but its milestone table gives scopes rather than completion state, dependency order, or a release gate. Chat/preference routes are listed without an M4 marker even though Chats is deferred to M4. [docs/PRD.md:14](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:14), [docs/PRD.md:601](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:601), [docs/PRD.md:684](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:684), [docs/PRD.md:795](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:795)

Live code confirms the core value loop is unavailable: ingestion only logs and the Worker registers no chat routes. [apps/api/src/lib/ingestion.ts:3](/Users/bhaskar/workspace/media-digest-assistant/apps/api/src/lib/ingestion.ts:3), [apps/api/src/index.ts:60](/Users/bhaskar/workspace/media-digest-assistant/apps/api/src/index.ts:60)

Impact: a polished catalog/read shell could be treated as releasable even though no real summaries can arrive and the “ask” experience does not exist.

Recommendation: add an implementation-status matrix per milestone/endpoint and define release profiles. A sensible minimum reader release is catalog + real M3 ingestion + digest/channel UI + hardening; chat can be a separate Intelligence release. Include a production checklist for secrets, CORS, crons, bindings, Vectorize metadata indexes, and end-to-end smoke tests.

### P1 — Confirmed: split follow writes can remain inconsistent indefinitely

The User DO is written first and Registry second; a partial failure is repaired only by a later follow/unfollow of that same pair. [docs/PRD.md:347](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:347), [apps/api/src/routes/follows.ts:126](/Users/bhaskar/workspace/media-digest-assistant/apps/api/src/routes/follows.ts:126), [apps/api/src/routes/follows.ts:155](/Users/bhaskar/workspace/media-digest-assistant/apps/api/src/routes/follows.ts:155)

Impact: Registry follower counts and automatic pause can disagree with the user-visible follow indefinitely. One failure can pause discovery despite an active follower, or continue spending credits after the last reader unfollows. This conflicts with the acceptance criterion that `followerCount` matches. [docs/PRD.md:703](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:703)

Recommendation: specify durable convergence: an outbox/operation ledger, scheduled reconciliation, or retryable idempotent coordinator. Add acceptance tests for failure after either write and recovery without requiring another user action.

### P1 — Confirmed: M3 is “approved for implementation” while its canonical outcome taxonomy is unresolved

The canonical schema still has `TODO(owner)` for transcript-size, embedding, and summary outcome identifiers. [docs/PRD.md:482](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:482), [docs/PRD.md:487](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:487)

Impact: migration checks, shared schemas, UI copy, attempt diagnostics, and tests cannot share a stable contract; different documents already use names such as `TRANSCRIPT_TOO_LARGE`, `AI_EMBED_FAILED`, and `AI_SUMMARY_FAILED`. [docs/specs/channel-simplification.md:202](/Users/bhaskar/workspace/media-digest-assistant/docs/specs/channel-simplification.md:202)

Recommendation: decide the complete enum before M3 implementation begins, place it in the canonical PRD, and make every subordinate spec/plan and shared schema use exactly that set.

### P2 — Confirmed: the privacy boundary contradicts the chat data flow

The PRD says chats and preferences never leave the User DO except in the user’s responses, but chat generation explicitly combines recent history and preferences and sends them to Workers AI. [docs/PRD.md:51](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:51), [docs/PRD.md:409](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:409)

Impact: the documented privacy promise is literally false and omits a material processing disclosure.

Recommendation: define the actual boundary: what message/history/preference data is transiently sent to Workers AI, whether it is retained or used for training, and what is never persisted or shared. Rewrite the promise around storage and access rather than claiming data never leaves the DO.

### P2 — Confirmed: M4 chat lifecycle and UX are not implementable from the PRD alone

The data model supports pending/failed assistant messages, but the functional requirements specify neither failure presentation nor retry/reconciliation/idempotency. The only history endpoint returns a bounded latest set with no cursor, despite the promise to preserve conversations. Preferences affect answers and have API routes, but no screen exposes them. [docs/PRD.md:398](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:398), [docs/PRD.md:406](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:406), [docs/PRD.md:450](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:450), [docs/PRD.md:601](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:601), [docs/PRD.md:684](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:684)

Impact: M4 implementations can diverge on stuck pending replies, duplicate questions, retry behavior, access to messages older than 50, untitled-chat labels, and preference editing.

Recommendation: write an M4 feature spec before implementation. Define the message state machine, recovery and duplicate-submit behavior, pagination, title defaults/rename policy, preference UI, errors, and corresponding acceptance criteria.

### P2 — Confirmed: unread totals and visible NEW markers are described inconsistently

The canonical behavior says unread counts span all eligible summaries while NEW applies only to returned digest items, yet Home says list-first loading makes unread counts and NEW markers “agree.” [docs/PRD.md:390](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:390), [docs/PRD.md:393](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:393), [docs/PRD.md:585](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:585)

Impact: a channel can correctly show 20 unread while a 24-hour digest contains only three NEW items; “agree” invites a false equality assertion and confusing UX.

Recommendation: state that both are taken from one pre-read snapshot, but the visible NEW set is a time-windowed subset of the all-history unread total. Add an acceptance case with older unread summaries outside the digest window.

### P2 — Confirmed: subordinate specs still contradict the canonical M3 contract

Examples:

- M3 says automatic provider blocks create no attempt row, while the PRD—and the same spec later—requires a finished blocked row. [docs/specs/m3-ingestion.md:174](/Users/bhaskar/workspace/media-digest-assistant/docs/specs/m3-ingestion.md:174), [docs/specs/m3-ingestion.md:192](/Users/bhaskar/workspace/media-digest-assistant/docs/specs/m3-ingestion.md:192), [docs/PRD.md:215](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:215)
- Channel simplification still includes a `waiting` channel count although the PRD removed it. [docs/specs/channel-simplification.md:48](/Users/bhaskar/workspace/media-digest-assistant/docs/specs/channel-simplification.md:48), [docs/specs/channel-simplification.md:277](/Users/bhaskar/workspace/media-digest-assistant/docs/specs/channel-simplification.md:277), [docs/PRD.md:252](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:252)
- Its Owner detail says Retry is disabled for any running attempt, while the PRD enables it after one hour so the route can reconcile a dead Workflow. [docs/specs/channel-simplification.md:536](/Users/bhaskar/workspace/media-digest-assistant/docs/specs/channel-simplification.md:536), [docs/PRD.md:633](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:633)

Impact: implementation agents following the approved spec/plan can build behavior the canonical PRD rejects.

Recommendation: perform a targeted cleanup of all active specs and plans before M3 coding, removing superseded statements rather than relying solely on precedence language.

### P2 — Open release question: provider capacity and environment isolation are undefined

The product has an unbounded shared catalog, retries every eligible unfinished episode for 48 hours, and relies on a 2,000-credit/month single provider. It defines only zero-credit blocking and a health display, not low-balance admission, prioritization, or expected workload. [docs/PRD.md:19](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:19), [docs/PRD.md:258](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:258), [docs/PRD.md:314](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:314)

The M3 spec also directs local `wrangler dev` to real Workers AI and Vectorize, while the PRD mandates one fixed namespace; no dev/production data separation is defined. [docs/specs/m3-ingestion.md:219](/Users/bhaskar/workspace/media-digest-assistant/docs/specs/m3-ingestion.md:219), [docs/specs/m3-ingestion.md:230](/Users/bhaskar/workspace/media-digest-assistant/docs/specs/m3-ingestion.md:230), [docs/PRD.md:531](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:531)

Impact: one burst or captionless channel can consume a disproportionate share of credits, and manual local validation could contaminate production retrieval data.

Recommendation: establish a capacity model, low-credit policy, first-attempt versus retry priority, and monthly alert threshold. Use separate dev/prod Vectorize indexes while retaining `shared-catalog` inside each environment, and make index/metadata readiness a deployment gate.

### P3 — Open product question: long-term scale and success quality lack measurable bounds

Channels, chats, messages, summaries, and vectors are retained indefinitely, while catalog and chat list resources are unpaginated and the only product success measure is Home latency for 20 followed channels. [docs/PRD.md:31](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:31), [docs/PRD.md:36](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:36), [docs/PRD.md:528](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:528), [docs/PRD.md:668](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:668), [docs/PRD.md:684](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:684)

Impact: “long-lived” is not backed by supported catalog/chat/history sizes, and technical completion does not establish summary usefulness or citation quality.

Recommendation: declare target scale ceilings and cursor-pagination triggers, plus a small manual quality bar for summary usefulness, timestamp validity, citation grounding, and owner workload. No analytics SDK is required.

## Notable strengths

- Clear canonical precedence and explicit separation of target requirements from current implementation.
- Strong separation of channel approval, episode content state, recovery state, and attempt diagnostics.
- Thoughtful recovery behavior: universal deadline, final real-cause attempt, Workflow reconciliation, and channel-independent repair.
- Excellent generation-safe publication and retrieval boundaries.
- Detailed role-specific screen and API mappings, including declined/re-approved preservation semantics.
- Concrete non-goals and dependency constraints substantially reduce scope drift.

## Not verified

- I did not run tests, builds, `wrangler dev`, or a browser walkthrough; this was analysis-only.
- I did not perform an exhaustive code-to-PRD audit; code inspection was limited to identity, route registration, ingestion placeholders, digest reads, and split follow writes.
- I did not verify deployed Cloudflare resources, CORS, secrets, crons, Vectorize metadata indexes, current DownSub behavior/credits, or provider legal/privacy terms.
- The working tree was clean at review time.

<oai-mem-citation>
<citation_entries>
MEMORY.md:219-220|note=[used prior review preferences to cover PM scope and cross-document implementation alignment]
</citation_entries>
<rollout_ids>
01a07d9d-f75c-7951-b2bd-50039e3c4b2a
</rollout_ids>
</oai-mem-citation>
```

### Replied · 2026-09-12 16:48

```text
The target has several strong correctness mechanisms—single-writer Registry transactions, attempt ledgers, staged vector generations, and post-query eligibility validation—but two architectural promises are currently unsafe as written: header-only identity provides no confidentiality or owner protection, and failed Vectorize cleanup can accumulate stale high-scoring generations until valid chunks are crowded out. I’m validating the remaining findings against schema and release-gate evidence now.
```

### Replied · 2026-09-12 16:49

```text
Architecture review complete. No files were edited; the worktree remains clean.

## PRD design defects

### P0 — Header-only identity permits complete account and owner impersonation

Anchors: `docs/PRD.md:59-65`, `docs/PRD.md:71-79`, `apps/api/src/middleware/user.ts:15-26`, `apps/api/src/middleware/owner.ts:6-23`, `apps/api/wrangler.jsonc:9`.

Failure scenario: any caller can supply another person’s normalized email and is routed to that person’s User DO. If the owner email is known or guessed, the same request obtains the seeded owner role and can approve, decline, pause, retry, skip, and enumerate follower emails. CORS does not protect direct HTTP clients, `workers_dev` is public, and no rate limit prevents identity probing or catalog flooding. This contradicts the assertion that another user’s private data is never exposed.

Recommendation: establish a real deployment trust boundary before production—Cloudflare Access, signed identity headers from a trusted gateway, or another authenticated edge. Bind the asserted identity cryptographically to the request, add basic abuse controls, and define owner revocation. If header-only identity is intentionally retained, restrict the Worker from public ingress and explicitly document that the system provides neither confidentiality nor authorization.

### P1 — Mirrored follows have no guaranteed convergence

Anchors: `docs/PRD.md:347-351`, `docs/PRD.md:356-360`, `docs/PRD.md:703-705`, `apps/api/src/routes/follows.ts:126-130`, `apps/api/src/routes/follows.ts:155-159`, `apps/api/src/routes/channels.ts:573-579`.

Failure scenario: the User DO write succeeds and the Registry write fails. The UI then treats the user as following, while follower count and automatic pause use the opposite state indefinitely. A hidden, already-active Follow control may never generate the “next follow” that the PRD relies upon for repair. The reverse inconsistency prevents system pause and spends ingestion resources for a channel nobody considers followed. A decline race can also leave a User DO follow active after a rejected follow request.

Recommendation: make one record canonical—preferably Registry membership, since it is already shared and owns pause—or add a durable, versioned saga/outbox with automatic reconciliation and read repair. Do not depend on a user repeating the same mutation.

### P1 — Failed vector cleanup can permanently degrade retrieval

Anchors: `docs/PRD.md:321-333`, `docs/PRD.md:547-552`, `docs/PRD.md:528-529`.

Failure scenario: replacement generation B activates but deletion of A fails. A later replacement C deletes B, not orphan A. Repeated failures accumulate highly similar inactive chunks; they can occupy Vectorize’s candidate window, be rejected during validation, and crowd out the active generation. “Fetch more” has no specified bound or cursor strategy. The retention statement that all vectors are retained also conflicts with physical deletion of superseded generations.

Recommendation: persist cleanup obligations in Registry storage and retry them from a bounded cleanup sweep until acknowledged. Track each retired generation and chunk count, expose backlog metrics, define bounded oversampling behavior, and clarify that retention applies to the active content generation—not superseded vectors.

### P1 — The canonical PRD lacks an enforceable single-flight/current-attempt invariant

Anchors: `docs/PRD.md:199-206`, `docs/PRD.md:271-289`, `docs/PRD.md:430-433`, `docs/PRD.md:500-501`.

Failure scenario: recovery and Owner Retry both pass pre-flight and create running attempts. The schema does not prohibit two running attempts for one video or identify the episode’s current attempt. Different generations can then stage concurrently, and a stale Workflow can publish after reconciliation or a later retry. The subordinate M3 spec describes an attempt gate, but this critical invariant is absent from the canonical schema and acceptance criteria.

Recommendation: require one atomic Registry `beginAttempt` transaction, add a partial unique index on `episode_ingestion_attempts(video_id) WHERE status='running'` or an explicit `current_attempt_id`, and require every mutation/publication to compare attempt ID, running status, recovery mode, and generation inside one transaction. Lift the stale-attempt test into PRD §8.

### P1 — The privacy promise conflicts with the chat data flow

Anchors: `docs/PRD.md:51-52`, `docs/PRD.md:398-410`.

Failure scenario: chat history and preferences must leave the User DO and be sent through the Worker to Workers AI, despite the PRD promising that private data never leaves the User DO except in the user’s responses. An implementation cannot satisfy both requirements, and operators cannot reason accurately about processing, logging, or retention.

Recommendation: state the actual boundary: private data is not persisted in shared storage but is transiently processed by the Worker and Workers AI for that user. Specify which history/preferences are sent, maximum context, redaction/logging rules, and required Cloudflare data-handling assumptions.

### P2 — Chat submission has no idempotency or pending-message recovery contract

Anchors: `docs/PRD.md:398-410`, `docs/PRD.md:450-458`, `docs/PRD.md:684-686`, `apps/api/src/do/user/chats.ts:118-170`, `apps/api/src/do/user/chats.ts:216-236`.

Failure scenario: the Worker stores a completed question and pending assistant reply, then times out or is cancelled during retrieval/AI. The reply remains pending forever. If the client retries after an ambiguous response, a second question/reply pair is created and AI cost is duplicated.

Recommendation: accept a client operation/idempotency key with a unique constraint, make completion conditional and idempotent, and define a pending lease plus reconciliation path. For durable guarantees, run generation through a Workflow or queue keyed by assistant message ID.

### P2 — “Marked read when returned” cannot be guaranteed by the current GET contract

Anchors: `docs/PRD.md:390-394`, `docs/PRD.md:675`, `docs/PRD.md:683`, `apps/api/src/routes/channels.ts:397-406`, `apps/api/src/routes/digest.ts:56-71`.

Failure scenario: the server commits `markRead`, then the connection fails before the response reaches the browser. The summary is considered read although the user never received it. The inverse ordering would return content without a receipt. HTTP delivery and a User DO write cannot be atomic.

Recommendation: either redefine the semantic as “marked read when fetched by the server” or add an explicit acknowledgement/read endpoint called after rendering. Test retry and connection-failure behavior against the chosen contract.

### P2 — Recovery fan-out, provider credits, and retained data are unbounded

Anchors: `docs/PRD.md:117-124`, `docs/PRD.md:207-210`, `docs/PRD.md:258-270`, `docs/PRD.md:314-316`, `docs/PRD.md:528-529`, `docs/PRD.md:792`.

Failure scenario: a backlog of captionless or transiently failing videos creates up to roughly nine starts per episode over 48 hours, all selected by one global recovery tick. Captionless checks consume credits, so a modest burst can exhaust the 2,000-credit monthly allowance. One Registry DO also accumulates all channels, episodes, attempts, summaries, and discovery history without pagination, retention, or capacity thresholds.

Recommendation: define per-tick launch limits and cursor-based carryover, maximum catalog/import values, monthly credit reservation/admission rules, pagination for retained collections, database/vector capacity alarms, and a backup/export/retention policy.

### P2 — Attempt outcome identifiers are unresolved in the canonical schema

Anchor: `docs/PRD.md:482-488`.

Failure scenario: migration constraints, TypeScript enums, UI copy, retry classification, and tests can choose different identifiers for transcript-size, embedding, and summary failures. Once persisted, renaming them conflicts with additive migration governance.

Recommendation: resolve every outcome identifier before rewriting `0001`, centralize the enum in shared code, mirror it in SQL checks, and add one transition test per code.

### P3 — Operability is mostly reactive and lacks recovery objectives

Anchors: `docs/PRD.md:36`, `docs/PRD.md:667`, `apps/api/src/index.ts:31-45`.

Failure scenario: `/health` reports OK while owner seeding, Registry migrations, Workflows, AI, Vectorize, or DownSub are unusable. The owner discovers failures only through UI state after recovery windows expire. There are no stated backlog, stuck-attempt, cleanup, latency, or credit thresholds.

Recommendation: define structured event names and correlation fields (`runId`, `attemptId`, `workflowId`, `generationId`), operational counters, alert thresholds, and backup/rebuild procedures. Keep liveness cheap, but add owner-visible readiness/dependency diagnostics.

## Implementation gaps against the target PRD

### P1 — Current main is still the obsolete pre-M3 architecture

Anchors: `docs/PRD.md:14-15`, `docs/PRD.md:430-433`, `docs/PRD.md:523-527`, `apps/api/src/lib/ingestion.ts:3-12`, `apps/api/wrangler.jsonc:21-35`, `apps/api/migrations/registry/0001_init.sql:35-37`, `apps/api/migrations/registry/0001_init.sql:64-152`, `apps/api/migrations/registry/index.ts:1-14`, `packages/shared/src/index.ts:84-145`, `apps/api/src/routes/channels.ts:424-451`, `apps/api/test/registry-migrations.test.ts:33-48`.

Failure scenario: deploying current main provides no Workflow, transcript, AI, Vectorize, or cron bindings. Ingestion only logs. The database and API still use channel-owned runs, `waiting_code`, three-attempt wording, old skip reasons, publication-time digest selection, and retry restrictions that directly contradict v4. Tests actively assert the obsolete two-migration schema.

Recommendation: treat the M3 schema, Registry stores, shared types, routes, fakes, workflow, and Wrangler bindings as one release-blocking migration. Do not expose a mixed contract. Because the repository is documented as undeployed, verify that fact again immediately before replacing `0001` and deleting `0002`.

### P2 — The advertised finish gate does not include all release-critical checks

Anchors: `docs/PRD.md:742-750`, `package.json:18-20`, `turbo.json:3-24`.

Failure scenario: `pnpm check` can pass without running the web production build, verifying that Zod stayed out of the bundle, testing Pages deep-link fallback, or exercising scheduled Worker behavior. There is also no checked-in CI workflow enforcing these gates.

Recommendation: create a reproducible release command/CI job that runs `pnpm check --force`, `pnpm build --force`, the bundle-content assertion, migration/OpenAPI checks, and local scheduled/deep-link smoke tests. Keep external-service probes as an explicit pre-deploy checklist.

## Architectural strengths

- Shared versus private ownership is clearly separated, with local transactions and no cross-DO SQL assumptions: `docs/PRD.md:93-105`, `docs/PRD.md:127-130`.
- Discovery history and episode processing are cleanly decoupled, and attempt ledgers make retries diagnosable: `docs/PRD.md:169-206`.
- Publication stages and verifies vectors before atomically exposing a summary/generation: `docs/PRD.md:319-333`.
- Retrieval retains the channel filter and revalidates channel, episode, and generation after Vectorize: `docs/PRD.md:531-552`.
- Schema constraints, additive migration governance, and explicit index planning are unusually concrete: `docs/PRD.md:460-527`.
- Shared Zod contracts plus generated OpenAPI provide a strong contract boundary: `docs/PRD.md:640-662`.
- Real DO/SQLite tests with env-selected external fakes are the correct testing seam: `docs/PRD.md:742-750`.

## Verification performed

- `pnpm typecheck --force`: passed, 4/4 tasks.
- `pnpm lint --force`: passed, 4/4 tasks with no warnings.
- `pnpm build --force`: passed.
- No `zod` references found in `apps/web/dist`.
- `git diff --check`: passed; worktree clean.
- `pnpm check --force`: attempted, but Workers tests could not bind `127.0.0.1` or write Wrangler’s user-level log under the restricted sandbox. This is environmental, not evidence of a test failure.
- Not run: `wrangler dev`, scheduled-trigger exercises, browser/deep-link walkthroughs, remote Cloudflare services, DownSub, Workers AI, or Vectorize. No network calls were made.

<oai-mem-citation>
<citation_entries>
MEMORY.md:35-36|note=[used prior M3 notes only as pointers and rechecked current files]
MEMORY.md:52-52|note=[confirmed historical documentation work was not proof of runtime implementation]
MEMORY.md:79-79|note=[recognized the restricted Wrangler test failure as environmental]
MEMORY.md:291-300|note=[followed prior review guidance to cross-check PRD code schema and tests]
</citation_entries>
<rollout_ids>
01a08458-61f3-74a2-bd5d-e209b5dfacdc
01a07c8b-6487-7b12-b7ba-2fbf7cdda6dd
</rollout_ids>
</oai-mem-citation>
```

### Replied · 2026-09-12 16:51

```text
## Overall verdict

The PRD has a strong core model—especially the separation of channel approval, episode state, recovery attempts, and staged vector publication. It is not yet implementation- or production-ready, mainly because of the trust boundary, cross-DO consistency, unresolved M3 contracts, and underspecified M4 experience.

No files were changed.

## A. Product manager review

1. **P0 — Privacy and owner authorization cannot be enforced.** Any client chooses `X-User-Email`; unknown emails auto-register, CORS is explicitly not a guard, and the header selects both the User DO and owner role. This contradicts the claims that private activity is isolated and owner operations are protected. See [PRD.md:51](</Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:51>), [PRD.md:59](</Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:59>), and [PRD.md:696](</Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:696>).  
   **Recommendation:** make this a deployment go/no-go decision: either establish authenticated/private ingress, or explicitly state that confidentiality and authorization rely entirely on cooperative trusted clients. Also define owner revocation; promote-only seeding accumulates owners.

2. **P1 — The usable-release cut line is unclear.** The PRD identifies M3/M4 as unbuilt, but milestones describe scope rather than current state, dependencies, or releasability. Current ingestion only logs and no chat routes are registered: [ingestion.ts:3](</Users/bhaskar/workspace/media-digest-assistant/apps/api/src/lib/ingestion.ts:3>), [index.ts:60](</Users/bhaskar/workspace/media-digest-assistant/apps/api/src/index.ts:60>).  
   **Recommendation:** add a status/dependency matrix and define a minimum reader release. Catalog UI without real ingestion cannot deliver the product’s core value.

3. **P1 — M3’s canonical contract remains unsettled.** The PRD leaves several persisted attempt codes as `TODO(owner)` at [PRD.md:482](</Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:482>). Active specs also contradict it: automatic provider blocks create no row at [m3-ingestion.md:174](</Users/bhaskar/workspace/media-digest-assistant/docs/specs/m3-ingestion.md:174>), while the PRD requires one; stale `waiting` counts remain at [channel-simplification.md:277](</Users/bhaskar/workspace/media-digest-assistant/docs/specs/channel-simplification.md:277>) and [home-read-experience.md:641](</Users/bhaskar/workspace/media-digest-assistant/docs/specs/home-read-experience.md:641>).  
   **Recommendation:** finalize the complete outcome enum and reconcile every active spec and plan before implementing the rewritten schema.

4. **P2 — “Long-lived” lacks operating bounds.** Data is retained indefinitely, collections are largely unpaginated, initial import has no maximum, and recovery shares a 2,000-credit monthly provider allowance. See [PRD.md:314](</Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:314>), [PRD.md:528](</Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:528>), and [PRD.md:792](</Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:792>).  
   **Recommendation:** define expected catalog/history sizes, pagination, per-tick work limits, credit reservation priorities, and alert thresholds.

## B. Product designer review

1. **P1 — M4 chats are not specified as a usable interaction.** The model includes pending/failed messages, but the PRD omits chat ordering, default titles, preference editing, pending/failure/retry states, duplicate submission behavior, older-history navigation, composer keyboard behavior, and source presentation. See [PRD.md:396](</Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:396>), [PRD.md:450](</Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:450>), and [PRD.md:601](</Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:601>).  
   **Recommendation:** write a focused M4 interaction specification before implementation.

2. **P2 — The owner attention link bypasses work it counts.** Requested channels contribute to the card, but its single link goes to `#attention`; requests are under `#requests`. See [PRD.md:576](</Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:576>) and [PRD.md:611](</Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:611>).  
   **Recommendation:** link each fact to its section, or route the CTA to the first non-empty section.

3. **P2 — Digest terminology obscures its ordering.** “Today’s digest” is ordered by when summaries became available, so an old video imported today can appear first while displaying only an old publication date. The seven-day view retains the same heading. See [PRD.md:384](</Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:384>) and [PRD.md:581](</Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:581>).  
   **Recommendation:** use “New summaries,” adapt the heading to the selected range, and show “summarised X ago” alongside publication age when they differ materially.

4. **P2 — Unread and NEW semantics contradict each other.** Unread counts span all eligible history, while NEW markers cover only returned, time-windowed items; nevertheless the UI section says they “agree.” See [PRD.md:390](</Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:390>) and [PRD.md:585](</Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:585>).  
   **Recommendation:** call the row value “total unread” and describe visible NEW items as a time-windowed subset captured from the same snapshot.

5. **P2 — Read state is changed before successful presentation.** Digest and channel GETs mark summaries read server-side, so a lost response, render failure, or speculative fetch can clear unread state without the person seeing the content. See [PRD.md:389](</Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:389>) and [PRD.md:675](</Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:675>).  
   **Recommendation:** either rename the semantic to “fetched,” or acknowledge rendered IDs through an idempotent read endpoint.

6. **P2 — Accessibility is absent from the canonical acceptance criteria.** “Text only” does not cover keyboard completion, focus after dynamic changes, announcements, contrast, zoom/reflow, or wide owner tables.  
   **Recommendation:** add M5 acceptance checks for keyboard operation, focus management, semantic status announcements, visible focus, 200% zoom, and narrow viewports.

## C. Engineering and architecture review

1. **P1 — Mirrored follow records have no guaranteed convergence.** The User DO is written first and the Registry second; a partial failure is repaired only if the same user later repeats a follow mutation. Until then, visible follow state, follower count, pause state, and ingestion spending can disagree. See [PRD.md:347](</Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:347>) and [PRD.md:703](</Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:703>).  
   **Recommendation:** make one record canonical or add a durable versioned outbox/reconciliation process. Do not depend on future user action.

2. **P1 — Single-flight episode processing is not a canonical invariant.** The schema neither points to a current attempt nor prevents two `running` attempts for one video. Recovery and Owner Retry can therefore race unless every starter relies on an unstated atomic check-and-insert rule. See [PRD.md:199](</Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:199>) and [PRD.md:430](</Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:430>).  
   **Recommendation:** require an atomic `beginAttempt`, plus either a partial unique running-attempt index or explicit `current_attempt_id`; fence every write by attempt ID, recovery mode, and generation.

3. **P1 — Failed vector cleanup can leak retired generations permanently.** Activation succeeds before cleanup; cleanup failure is only logged. A later replacement deletes the immediately previous generation, not necessarily older orphaned ones. Those vectors can consume candidate slots and progressively reduce retrieval quality. See [PRD.md:321](</Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:321>) and [PRD.md:547](</Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:547>).  
   **Recommendation:** persist cleanup obligations and retry them through a bounded sweep; expose backlog health and define oversampling limits.

4. **P1 — The stated privacy boundary contradicts the AI flow.** The PRD says chats and preferences never leave the User DO, but chat generation necessarily sends history and preferences through the Worker to Workers AI. See [PRD.md:51](</Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:51>) and [PRD.md:409](</Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md:409>).  
   **Recommendation:** describe the real boundary: private data is not stored in shared persistence, but selected content is transiently processed by Workers AI. Define context limits and data-handling assumptions.

5. **P2 — Chat processing lacks idempotency and recovery.** An interrupted request can leave an assistant message pending forever; retrying an ambiguous response can duplicate the question and AI cost.  
   **Recommendation:** accept an operation key, make completion conditional and idempotent, and define a pending lease/reconciliation path—potentially a Workflow keyed by assistant-message ID.

## Notable strengths

- Excellent separation of approval, discovery, content, and recovery state.
- Thoughtful 48-hour recovery and lost-Workflow reconciliation.
- Strong staged-generation publication and retrieval validation.
- Clear shared/private storage ownership and eligibility filtering.
- Concrete schema constraints, migration rules, typed API documentation, and test seams.
- Compact reader/owner information architecture with sensible preservation through decline, unfollow, and re-approval.

Supporting review found the working tree clean; typecheck, lint, and build passed. Full `pnpm check --force` could not complete because the restricted sandbox blocked the Workers test listener/log path. No browser, scheduled-trigger, DownSub, Workers AI, or Vectorize verification was performed.

Historical notes were used only to identify previously sensitive lifecycle areas; all conclusions above were rechecked against the current files.

<oai-mem-citation>
<citation_entries>
MEMORY.md:35-36|note=[required rechecking current PRD and specs]
MEMORY.md:219-220|note=[review scope included UX PM consistency and implementation alignment]
rollout_summaries/2026-09-09T04-06-19-v5AE-m3_ingestion_ux_contract_alignment.md:21-30|note=[historical lifecycle decisions used as review context]
rollout_summaries/2026-09-07T20-44-59-F494-home_owner_product_spec_review.md:28-33|note=[prior release and UX gaps revalidated against current files]
</citation_entries>
<rollout_ids>
01a08458-61f3-74a2-bd5d-e209b5dfacdc
01a07d9d-f75c-7951-b2bd-50039e3c4b2a
</rollout_ids>
</oai-mem-citation>
```

### Asked · 2026-09-12 18:32

```text
clear
```
