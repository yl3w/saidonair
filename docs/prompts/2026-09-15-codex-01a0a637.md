# UI feedback, worked in Codex

2026-09-15 14:04 · Codex · 6 prompts, 27 turns · session `01a0a637`

<!-- capture:covers codex-01a0a637 -->

The same fifteen-item feedback list the Claude Code session worked, started here first — including the reader losing its navigation context when an episode is opened from History.

---

### Asked · 2026-09-15 14:04

```text
As a UI designer and Front end engineer working on Said on Air I want to address feedback we received. There are over 15 items we will tackle one at a time. Review the design and prodict requirement and let me know when you are ready to continue
```

### Replied · 2026-09-15 14:04

```text
I’m using the repo’s `using-superpowers` workflow so I follow its working conventions, then I’ll read the canonical `docs/PRD.md` and `docs/design.md` in full. I’ll only review and report readiness—no changes yet.
```

### Replied · 2026-09-15 14:05

```text
The working tree is clean. I’m now cross-reading the product contract and design guide, with particular attention to screen states, interaction rules, accessibility, responsive behavior, and the boundary between shipped behavior and later milestones.
```

### Replied · 2026-09-15 14:06

```text
I’m ready to continue. I reviewed the current:

- [Product requirements](/Users/bhaskar/workspace/media-digest-assistant/docs/PRD.md)
- [Design guide](/Users/bhaskar/workspace/media-digest-assistant/docs/design.md)
- [Design-phase specification](/Users/bhaskar/workspace/media-digest-assistant/docs/specs/design-phase.md)
- [Design-phase implementation record](/Users/bhaskar/workspace/media-digest-assistant/docs/specs/design-phase-plan.md)

I’ll assess each feedback item through both a UI-design and frontend-engineering lens: product-contract alignment, usability, responsive behavior, accessibility, all four data states, scale, copy, component consistency, and implementation risk. I’ll treat the PRD as authoritative, followed by the current design guide; notably, themes and reading preferences now apply across the whole product despite older wording in the phase specification.

No files were changed. Send the first item whenever you’re ready.

<oai-mem-citation>
<citation_entries>
MEMORY.md:84-102|note=[Used prior review context to recheck current canonical and subordinate documents]
</citation_entries>
<rollout_ids>
01a0975a-be08-7bd3-a68d-eae0ca2a9ed7
</rollout_ids>
</oai-mem-citation>
```

### Asked · 2026-09-15 14:06

```text
1. The reader loses the user’s navigation context

     Opening an episode from History still makes the back button say “Back
     to the queue” and routes to Queue. Related episodes behave the same
     way. This breaks spatial continuity and loses the user’s History
     position.

     “Done” also appears on already-read episodes. Pressing it silently
     advances to the next unread episode, which is surprising when the
     user arrived from History.

     Recommendation: preserve the originating route and scroll position.
     On read episodes, replace “Done” with “Next unread” or simply omit
     it. See apps/web/src/screens/Reading.tsx:61.
```

### Replied · 2026-09-15 14:06

```text
I’m using the `brainstorming` workflow because this feedback changes navigation and read-state behavior. I’ll first verify the current Reading flow and its product/design contracts, then propose a narrow plan for your approval before editing anything.
```

### Replied · 2026-09-15 14:06

```text
This looks bounded: the Reading flow already exists, so I’ll keep the design in chat rather than create a new spec. I’m checking the current route state, read-status logic, and recent history before I ask the one product choice that affects the implementation.
```

### Replied · 2026-09-15 14:08

```text
Confirmed—the feedback is valid:

- Back is hard-coded to `/queue`.
- Related links carry no origin context.
- “Done” renders for both read and unread episodes.
- Returning through normal browser history can restore History’s scroll position; routing anew to History cannot.

For already-read episodes, I recommend omitting the action. “Next unread” introduces unrelated queue navigation into a History reading flow and adds chrome to the intentionally minimal reading view.

Should I proceed with no action on read episodes, while unread episodes retain “Done”?
```

### Asked · 2026-09-15 14:09

```text
please implement changes and commit
```

### Replied · 2026-09-15 14:09

```text
I’ll implement the approved bounded design: preserve the entry route through related episodes, return via browser history so History restores its position, fall back safely for cold links, and show “Done” only while the episode is unread. I’m using the repo’s debugging, TDD, and completion-verification workflows; because this web app intentionally has no component tests, I’ll validate with the prescribed static gates and an interactive browser walkthrough.
```

### Replied · 2026-09-15 14:10

```text
Root cause is confirmed: the Reading screen discards the route that opened it, always pushes `/queue`, renders “Done” whenever a read field exists, and pushes every related episode as another history entry. I’ll fix that at the link/route boundary rather than add scroll bookkeeping: ordinary in-app opens record the origin, related/next navigation replaces the current reading entry, and Back pops to the original list so the browser restores its exact scroll position. Modified-click/new-tab opens remain clean cold links with Queue as the fallback.
```

### Replied · 2026-09-15 14:11

```text
The first static pass caught two implementation issues before runtime testing: TypeScript would not retain the optional-origin narrowing, and intercepted links must stop propagation because `preact-iso` also listens globally for anchor clicks. I’m correcting both, then I’ll rerun the focused gates before the browser walkthrough.
```

### Replied · 2026-09-15 14:12

```text
The focused TypeScript, lint, and whitespace gates are now clean. Next I’m exercising the real SPA behavior under `pnpm dev`: History → read → Back, History → read → related → Back, the already-read action state, and cold-link fallback.
```

### Replied · 2026-09-15 14:13

```text
The local Vite and Wrangler servers are healthy, but this session has no browser surface or installed headless browser, so I cannot complete the click-through here. I’m continuing with the repo’s required full check and a focused code review; I’ll be explicit about that runtime limitation in the handoff rather than claim a visual walkthrough occurred.
```

### Replied · 2026-09-15 14:13

```text
I’m invoking the completion review now because the navigation change touches browser history semantics that static typing cannot validate. The reviewer will inspect the uncommitted diff against the approved behavior and repository contracts while I run the full project gate.
```

### Replied · 2026-09-15 14:13

```text
I’m using the repository’s `using-superpowers` workflow to honor its review process. I’ll keep this strictly read-only and ground the verdict in the current diff, surrounding navigation code, and installed `preact-iso` behavior.
```

### Replied · 2026-09-15 14:13

```text
The diff’s core mechanism is clear: list links encode an origin, the reader uses native history for contextual Back, and related/Done navigation uses replacement. I’m now checking whether `preact-iso` actually preserves the browser history entry and scroll state in those exact combinations, plus every `SummaryRow` call site.
```

### Replied · 2026-09-15 14:16

```text
No Critical findings.

Important

1. [Reading.tsx:114](/Users/bhaskar/workspace/media-digest-assistant/apps/web/src/screens/Reading.tsx:114) delegates restoration entirely to browser Back, but History’s accumulated state is destroyed on navigation. `preact-iso` remounts a different route component, while [History.tsx:236](/Users/bhaskar/workspace/media-digest-assistant/apps/web/src/screens/History.tsx:236) initializes empty and fetches only the first 50 rows. If someone opens an episode after “Show more,” Back returns to `/history`, but that row and the preceding accumulated height no longer exist, so its scroll position cannot be restored. This directly misses the central History requirement.

2. [Reading.tsx:76](/Users/bhaskar/workspace/media-digest-assistant/apps/web/src/screens/Reading.tsx:76) advances with a replace navigation to another `/read/:episodeId` after setting `finishing` true, but never resets it on success. `preact-iso` deliberately preserves component state when only params change, as shown at [router.js:194](/Users/bhaskar/workspace/media-digest-assistant/apps/web/node_modules/preact-iso/src/router.js:194). Consequently, the next unread episode’s Done button at [Reading.tsx:176](/Users/bhaskar/workspace/media-digest-assistant/apps/web/src/screens/Reading.tsx:176) remains disabled and displays `…`, breaking repeated next-unread behavior.

3. [Reading.tsx:316](/Users/bhaskar/workspace/media-digest-assistant/apps/web/src/screens/Reading.tsx:316) infers a live history context solely from the user-visible `from` query. Since [SummaryRow.tsx:81](/Users/bhaskar/workspace/media-digest-assistant/apps/web/src/components/SummaryRow.tsx:81) puts that query in the URL, copying the URL or opening it cold in another tab still causes [Reading.tsx:114](/Users/bhaskar/workspace/media-digest-assistant/apps/web/src/screens/Reading.tsx:114) to call `history.back()`, which can no-op or leave the app instead of safely falling back to Queue. Conversely, the validator rejects legitimate query-bearing list routes such as `/queue?note=owner-only`, produced at [session.tsx:121](/Users/bhaskar/workspace/media-digest-assistant/apps/web/src/session.tsx:121), losing their route and scroll context. A URL string alone cannot establish that the matching list entry exists immediately behind the reader.

Minor

- None.

The documentation and new visible strings are placed correctly: PRD, design guide, design-phase spec, and `lib/copy.ts` were all updated consistently.

Validation run: web TypeScript check, Biome check on changed implementation files, and `git diff --check`; all passed. No browser/runtime walkthrough or full `pnpm check` was run. The working tree remained unchanged.

Ready to commit: **No**.

<oai-mem-citation>
<citation_entries>
MEMORY.md:193-197|note=[Located prior repository context concerning related episodes]
</citation_entries>
<rollout_ids>
01a08458-61f3-74a2-bd5d-e209b5dfacdc
</rollout_ids>
</oai-mem-citation>
```

### Replied · 2026-09-15 14:16

```text
The review found three real gaps, so I’m not committing this version: History’s paginated rows would be lost on remount, the next unread inherited a stuck busy state, and a copied `?from=` URL could incorrectly leave the app. I’m tightening the design around actual browser history entries: cache History’s loaded pages for the current session, store origin/scroll metadata on the source entry itself, and treat query text only as a label/fallback—not proof that Back is safe.
```

### Asked · 2026-09-15 14:18

```text
As a UI designer and Front end engineer working on Said on Air I want to address feedback we received. There are over 15 items we will tackle one at a time. Review the design and prodict requirement and let me know when you are ready to continue
```

### Asked · 2026-09-15 14:19

```text
As a UI designer and Front end engineer working on Said on Air I want to address feedback we received. There are over 15 items we will tackle one at a time. Review the design and prodict requirement and let me know when you are ready to continue
```

### Asked · 2026-09-15 14:21

```text
please continue
```

### Replied · 2026-09-15 14:21

```text
I’m continuing the navigation fix and commit. The remaining work is to make History restoration reliable, finish the review corrections, and rerun validation.
```

### Replied · 2026-09-15 14:22

```text
History needs to reload all the pages the reader had opened before restoring the scroll position. I’m saving that page depth and the list controls on the browser history entry; this also lets read receipts refresh correctly after “Done.”
```

### Replied · 2026-09-15 14:23

```text
I’ll re-review the corrected tree from scratch, focusing on same-component route transitions (`/history/:day` and `/read/:episodeId`), history-entry state isolation, async reload/scroll timing, and stale Done operations. I’ll keep the repository untouched.
```

### Replied · 2026-09-15 14:25

```text
The corrections fix the original URL-spoofing and straightforward busy-state issues. Two lifecycle risks remain in the shared history-state hook and History’s uncancelled “Show more” request; I’m validating those against the exact `preact-iso` reconciliation behavior and checking the corrected files now.
```

### Replied · 2026-09-15 14:28

```text
Re-review verdict for the corrected snapshot: **not ready yet**.

No Critical findings.

Important

1. `useReadingReturnScroll` leaves a consumed return marker in `history.state`.

   At `apps/web/src/lib/reading-navigation.ts:80-93`, successful restoration only sets `pending.current = null`; it never removes `RETURN_STATE`. After returning from Reading once, the user can scroll elsewhere, navigate away, then browser-Back to the same list entry. The newly mounted hook reads the stale marker and overrides the browser’s newer restored position with the old reader-opening position.

2. `useReadingView` does not load saved state when a mounted component moves between history entries.

   At `apps/web/src/lib/reading-navigation.ts:8-27`, `history.state[VIEW_STATE]` is read only by the `useState` mount initializer. `preact-iso` preserves the component when `/history` changes to `/history/:day`. If two History entries acquire different page depth or calendar state, Back/Forward to the earlier entry can find `stored.key !== key`, use `initial`, and overwrite that entry’s saved state instead of restoring it. This is the primary remaining day-parameter lifecycle defect.

3. An old “Show more” response can corrupt a newly selected History day.

   In the corrected snapshot at `apps/web/src/screens/History.tsx:301-319`, `loadMore` has no cancellation or day/generation guard. If the user changes `/history/:day` while that request is pending, the old response can resolve after the new day’s initial fetch and append old-day episodes, cursor, and page count into the new view.

Minor

- The stale Done guard identifies navigation only by `episodeId` (`Reading.tsx:55-67`, checks at `84-87`). In an A → related B → related A sequence while A’s earlier Done flow remains pending, that old operation again matches `active.current` and may navigate the revisited A unexpectedly. A navigation-generation token would close this ABA race.

The previous issues are otherwise corrected: origin is no longer URL-spoofable, related/next navigation replaces the reader entry, read-state visibility is correct, and the normal next-unread busy state is reset.

One review-environment caveat: while I was reviewing, another process reset the tracked working tree to HEAD; the final observed status contained only `?? apps/web/src/lib/reading-navigation.ts`. I did not modify anything. Therefore I could inspect the corrected tracked diff captured before that reset, but could not validate that exact snapshot with the final typecheck/runtime state.

Ready to commit: **No — with the three Important lifecycle fixes, then reapply/recheck the full diff.**
```
