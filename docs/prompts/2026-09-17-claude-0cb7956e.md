# The README says what shipped

2026-09-17 18:06 → 2026-09-18 13:39 · Claude Code · 3 prompts, 10 turns · session `0cb7956e`

<!-- capture:covers claude-0cb7956e -->

Bringing the README and its architecture diagram back in line with the built product.

Commits in this window:

- `5b850a7` docs: the README and §3's diagram say what shipped
- `335eab6` docs: Chats is a destination, and the ruling that said it never would be

---

### Asked · 2026-09-17 18:06

```text
ensure the readme is updated to reflect current state of application
```

### Replied · 2026-09-17 18:06

```text
I'll start by reading the project instructions and the current README.
```

### Replied · 2026-09-17 18:07

```text
Now I have the picture. Writing the updated README:
```

### Replied · 2026-09-17 18:08

```text
`pnpm check` passes (8 tasks, 391 tests). README rewritten at `README.md`; I did not commit, per the repo's "commit only when asked" rule.

What was stale and is now current:

- **Status.** It said multi-chat retrieval was "planned for the next product milestone". M4 and M5 are both complete (owner-declared 2026-09-17), so the intro now says every feature milestone is built and what remains is M6, the sweep of PRD §8.
- **What it does.** Added chats — begun at a summary, per-message episode scope, cross-encoder relevance, citations taken from the retrieval rather than the model — and made discovery say it reads the uploads playlist, so Shorts and live streams never become episodes.
- **New Screens section.** The eight routes `main.tsx` actually serves (`/queue`, `/read/:episodeId`, `/history`, `/sources`, `/chats`, `/account`, `/curate`), with the one-navigation-for-everyone note. The old README described no screens at all, and the ones it implied — Home, Owner — no longer exist.
- **Architecture.** The diagram now shows the chat path (embed → filtered Vectorize → rerank → answer), which never touched Workflows, and the prose records that publication promotes the staged generation and deletes superseded ones (`workflows/ingest.ts:350-376`).
- **Technology.** Tailwind 4, daisyUI 5 with one custom theme, and Lucide were missing entirely; the three Workers AI model ids are now named, reranker included.
- **Commands.** Added `pnpm verify:vectorize` (with why `pnpm check` can't cover it) and `pnpm skills:install`, plus the `clean-local` note for starting local state over.
- **Repository structure.** Added `scripts/`, and `docs/` now mentions the design guide.

One thing I left alone deliberately: PRD §3's own architecture diagram is stale in the same way the README was — it shows the pre-Design-phase screens and says "chats in M4". Worth a separate fix if you want it.
```

### Asked · 2026-09-17 18:42

```text
i want to fix the architecture diagram
```

### Replied · 2026-09-17 18:43

```text
The diagram needs to match what shipped. Let me replace it:
```

### Replied · 2026-09-17 18:44

```text
The diagram is fixed (`docs/PRD.md:94`). What changed:

- **Screens** — the three stale lines (Account → Home → Channel details → Owner) are replaced by the eight routes that exist, with the fact that Reading is reached from a list and `Ask` is the only way into a chat.
- **Publication** — now ends `→ delete every generation the publication superseded` (29c7fcd).
- **Vectorize** — names all three per-environment indexes, not just `media-rag`.
- **Chat query** — was three steps ending at "Workers AI answer". Now carries the per-message episode scope, the `channelId` *or* `episodeId` filter (the filter really is a union in `lib/vectorize.ts`), the cross-encoder rerank above the relevance floor, and that the kept chunks are the sources rather than the model's citations.
- **"channel cron" → "discovery cron"**, matching §9's cadence entry.

Widest line is 115 chars, inside the file's existing 118. `pnpm check` passes. Not committed.

While checking the screen lines I hit a contradiction I can't resolve from the code, because two approved artifacts disagree:

- `Nav.tsx:26-30` ships **three** primary destinations — Queue, Sources, Chats — added in de875d0, and `docs/specs/m4-3-chat-web.md:43` (approved) says "`/chats` joins primary navigation for the first time".
- `docs/PRD.md:462` and `:786` say primary navigation is Queue and Sources and "still does not carry it", and §9's 2026-09-15 entry (`:1686`) says it was "made permanent... the navigation stays two items after M4 rather than three".

M4.3 §6 asserted "PRD §7 already describes these screens and needs no edit", which is what let the three spots survive.
```

### Replied · 2026-09-18 10:28

```text
Done. Four documents changed, nothing committed.

**`docs/PRD.md` §3 — the diagram** (the thing you asked for): screens replaced with the eight shipped routes plus how Reading and a chat are reached; publication now ends `→ delete every generation the publication superseded`; all three per-environment Vectorize indexes named; the chat query carries the per-message episode scope, the `channelId`-or-`episodeId` filter, the cross-encoder rerank, and that the kept chunks *are* the sources; "channel cron" → "discovery cron".

**The nav contradiction, amended as you chose:**
- `docs/PRD.md:462` (§4.5) — "primary navigation stays Queue and Sources" → Primary navigation carries Chats since M4.3, with the entry rule left intact: a destination is not an entry.
- `docs/PRD.md:787` (§7) — "primary navigation still does not carry it" → it does carry it, reversing that day's expectation.
- `docs/PRD.md:1690` (§9) — the 2026-09-15 ruling keeps its text, and the reversal is recorded under it: what changed (the destination), what did not (the item shipped in the same commit as the screen, `de875d0`), and why the claim survived two days — `m4-3-chat-web.md` §6 judged PRD §7 needed no edit.

**`docs/design.md:307`** was the same defect in the design guide, and it contradicted its own §7 ("three since Chats was built") — M4.3 §6 had already committed to making the line past tense, so I did that.

`pnpm check` passes. One thing I did not touch: `docs/PRD.md:784` still reads "Until M4 builds it there is no Chats destination… primary navigation is Queue and Sources" — that clause is scoped to before M4 and is the trail the reversal argues against, so it stands as history rather than a current claim. Say the word if you'd rather it go.
```

### Asked · 2026-09-18 13:38

```text
commit and push
```

### Replied · 2026-09-18 13:39

```text
Pushed to `main` (`3cc3a48..335eab6`), working tree clean. Split into two commits since they're separate changes:

- **5b850a7** — `README.md` and PRD §3's diagram: the doc-currency fix.
- **335eab6** — PRD §4.5, §7, §9 and `docs/design.md` §3: the Chats-destination reversal recorded, keeping the 2026-09-15 decision above it.
```
