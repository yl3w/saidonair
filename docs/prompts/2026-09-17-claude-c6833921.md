# Checking a new channel ingested

2026-09-17 11:58 · Claude Code · 1 prompt, 8 turns · session `c6833921`

<!-- capture:covers claude-c6833921 -->

One prompt, asking whether a newly added channel's workers and summaries had run.

---

### Asked · 2026-09-17 11:58

```text
I added a new channel UCzBZNjJdxTdviBI8KfnE66Q - can you check the worker statuses and episode summarization
```

### Replied · 2026-09-17 11:59

```text
I'll start by reading the project instructions, then check on the running workers.
```

### Replied · 2026-09-17 11:59

```text
Dev server is running (api on :8787, vite). Let me look at what routes exist for checking ingestion state.
```

### Replied · 2026-09-17 11:59

```text
Channel is in and all 5 episodes report available. Let me look at the episodes and their summaries.
```

### Replied · 2026-09-17 12:00

```text
All five landed clean. Let me check the second channel and run the repo's own summary evaluator against these published summaries.
```

### Replied · 2026-09-17 12:00

```text
Processing is clean across the board; summary quality shows some flags. Let me check the vector index and the worst-scoring episode's detail.
```

### Replied · 2026-09-17 12:02

```text
Let me check one thing that looks off — the digest's ordering.
```

### Replied · 2026-09-17 12:03

````text
Everything's healthy. Here's what I found.

## Workers

| | |
|---|---|
| `wrangler dev --env dev` (api :8787) | up, `/health` → `{"status":"ok"}` |
| vite (web :5173) | up |
| DownSub | `ok`, 2043 credits left |
| `media-rag-dev` | `pnpm verify:vectorize` passes — both `channelId` and `episodeId` metadata indexes present |

## The new channel — fully ingested, zero failures

**Otherworld** (`UCzBZNjJdxTdviBI8KfnE66Q`) went in at 11:55:26 today, approved and followed. One `initial` run, feed `read`, 5 discovered (the import cap), **all 5 `available`** — 0 pending, 0 failed, 0 skipped. Each took a single attempt, no retries, 172–199 s, all finishing between 11:58:19 and 11:58:45. Catalog-wide: 2 approved channels, 10 available episodes, attention list empty.

## Summarization works, but this channel scores worse than the other one

I ran the repo's own `scoreSummary` (`lib/summary-eval.ts`) over all 10 published summaries:

- **Knowledge Project**: 2 of 5 clean, coverage gaps 16–27%, 6–9 deciles covered.
- **Otherworld**: 0 of 5 clean, gaps **30–46%** (threshold is 25%), four of five covering only 4–5 deciles.

Two specific things worth your eye:

**1. Ep 42 (`ZfdOIf-GWp4`) loses its back half.** 39-min episode, budget 5 takeaways, 2 sections, quota [3,2]. Section 2's second pick came back with no parsable timestamp, and its text is a restatement of the executive summary's third sentence rather than a locatable point:

```
  138s   5%  Jack Wagner notes that skeptical explanations…
  741s  31%  Isish reports experiencing strange sensations…
 1021s  43%  Isish notes she has seen these shadowy figures…
 1243s  53%  Isish saw a shadow figure in her car…
  null   --  Isish's story highlights the need for empathy…
```

So the reader gets nothing after 20:43 of a 39-minute episode, plus one takeaway they can't jump to. It's the only null timestamp across all 10 episodes — `allocateTakeaways` spent the quota correctly; the map call just handed back a marker that `startSecOf` rejected.

**2. Anonymous takeaways on three of five** (3/6, 4/6, 4/10) — "The narrator's boyfriend…", "The host of the podcast raises…" — while the same summaries name Ally, Isish and Jack Wagner elsewhere. This looks content-shaped rather than a regression: Otherworld is anonymous-guest storytelling, so the transcripts often genuinely don't name people. Also one over-long tag, `"near death experience"` (prompt v3 asks for two words).

## One dev-environment note

Crons are production-only, so nothing here will pick up Otherworld's next upload on its own — it needs `POST /channels/{id}/runs` ("Check the feed now"). Worth knowing anyway: the channel's five newest long-form uploads are all from **January 2026**, so the queue will look stale no matter how often you check.

Nothing is broken, so I haven't changed anything. If you want, I can dig into whether the null-timestamp case deserves a code-side guard — dropping a markerless takeaway and taking the next one from the same section would have kept Ep 42's coverage — but that's a prompt/pipeline change, so I'd want your go-ahead first.
````
